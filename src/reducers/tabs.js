import {NEW_TAB, UPDATE_TAB} from '../actions/actionTypes';

const initialState = {}

export function tabs(state = initialState, action) {
  switch (action.type) {
    default:
      return state;
  }
}
